{
  "description": "Auto-starts chatroom server and injects instructions into subagent prompts",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Task",
        "hooks": [
          {
            "type": "command",
            "command": "bash ${CLAUDE_PLUGIN_ROOT}/hooks/scripts/task-pretool.sh",
            "timeout": 15000
          }
        ]
      }
    ]
  }
}
